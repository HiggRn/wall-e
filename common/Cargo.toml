[package]
name = "wall-e-common"
version = "0.1.0"
edition = "2024"

[dependencies]
postcard = { version = "1.0", features = ["use-std"] }
serde = { version = "1.0", default-features = false, features = ["derive"] }
